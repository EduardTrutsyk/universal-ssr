// Actions

// Action Creators

// Initial state
const INITIAL_STATE = {};

// Reducer
export default (state = INITIAL_STATE, action = {}) => {
  switch (action.type) {
    // do reducer stuff
    default: return state;
  }
};
